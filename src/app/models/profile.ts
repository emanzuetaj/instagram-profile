import { Counts } from './counts';

export class Profile {
    bio: string;
    full_name: string;
    id: string;
    is_business: boolean;
    profile_picture: string;
    username: string;
    website: string;
    counts: Counts;
}